<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.com.meyshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.EditProfileActivity"></activity>
        <activity android:name=".views.MyOrderDetailsActivity" />
        <activity android:name=".views.ProfilActivity" />
        <activity android:name=".views.SuccessBuyProductsActivity" />
        <activity android:name=".views.ProductsCheckoutActivity" />
        <activity android:name=".views.ProductsDetailsActivity" />
        <activity android:name=".views.SuccesRegisActivity" />
        <activity android:name=".views.GetStartedAct" />
        <activity android:name=".views.SigninActivity" />
        <activity android:name=".views.SignUpOneActivity" />
        <activity android:name=".views.SignUpTwoActivity" />
        <activity android:name=".views.HomeActivity" />
    </application>

</manifest>